import styled from 'styled-components';
import { baseContainerBlockStyles } from '@/styles/base';

const AddTodoItemContainer = styled.div`
  ${baseContainerBlockStyles}
`;

export default AddTodoItemContainer;
